- if object.errors.any?
  .alert.alert-error
    %a.close{ data: { dismiss: 'alert' } } &#215;
    %h3= t('simple_form.error_notification.default_message')
    %ul
      - object.errors.each do |attribute, message|
        - if message.present?
          %li= object.errors.full_message(attribute, message)
